**What & why:**

**Linked issue:**

**How tested:**
<!-- There is no end-to-end/instrumentation suite yet — also say what you ran by hand. -->
- [ ] `gradlew.bat assembleDebug` succeeds
- [ ] `pnpm build` succeeds in `windows/`
- [ ] `pnpm test` passes in `windows/`
- [ ] Exercised the change in the running app
- [ ] If sync was touched: paired a device and confirmed entries land on both sides
- [ ] If a module/feature was added or removed: README lists updated
